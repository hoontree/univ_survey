/** 폼 입력 공통 스타일 — 관리자 폼과 본인 확인 게이트가 함께 쓴다. */
export const inputStyle = {
  background: "var(--space-800)",
  color: "var(--text-strong)",
  border: "1px solid var(--border-strong)",
  borderRadius: "var(--radius-md)",
  padding: "10px 12px",
  fontSize: "var(--text-sm)",
} as const;
